# 部署包契约

## 固定层

- `python3.11`
- `.venv`
- `start.sh`
- `MCP_HOST`
- `MCP_PORT`
- `MCP_SSE_PATH`
- `mcp_template/`

## 可变层

- `business/` 下的业务文件
- 业务规则
- 工具名
- 返回字段
- 业务说明文档

## 生成原则

1. 固定层不随业务变化而变化。
2. 可变层必须按当前业务重新生成。
3. 新业务只替换 `business/` 和业务逻辑文件。
4. 部署说明必须明确“哪些可复用，哪些不可复用”。
