# 迭代上下文（进入 merger / correction 前必读）

## 本文作用

约定迭代时先干什么、产出什么，避免 Agent 只读了合并/纠正模板却转去跑专利点分析。

## 何时读本文

用户明显在已有交底书或上一轮交付稿上继续工作时，在 `Read` `merger.md` 或 `correction_handler.md` 之前先读本文。

## 输入与输出

- 输入：本轮说明、用户 @ 的文件、当前基准交底书
- 输出：写入新时间戳文件，不得覆盖旧稿；并追加 `交底书修订对话记录.md`
