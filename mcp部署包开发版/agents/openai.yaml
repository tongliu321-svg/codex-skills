interface:
  display_name: "mcp部署包开发版"
  short_description: "生成可直接交给开发同事的标准MCP部署包"
  brand_color: "#0F766E"
  default_prompt: "使用 $mcp部署包开发版 直接在桌面生成可交给开发同事部署的标准MCP部署包；不要只输出说明，必须产出完整部署目录。"

policy:
  allow_implicit_invocation: true
