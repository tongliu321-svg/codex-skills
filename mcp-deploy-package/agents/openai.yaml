interface:
  display_name: "MCP 部署包"
  short_description: "生成公司标准 MCP 部署包"
  default_prompt: "Use $mcp-deploy-package to generate a standard MCP deployment package from the provided business requirements, business logic, and business files."

policy:
  allow_implicit_invocation: true
